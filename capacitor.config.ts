import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.test',
  appName: 'ionic-version-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
